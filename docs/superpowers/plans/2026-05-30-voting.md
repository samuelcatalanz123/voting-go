# App de votaciones — Plan

**Goal:** App web en Go para crear encuestas, votar y ver resultados con barras.
**Tech:** Go (net/http, html/template, go:embed, crypto/rand, sync). Sin deps externas.
Módulo: `github.com/samuelcatalanz123/voting-go`. Commits autoría Samuel.

## Tareas
1. **poll (con prueba):** `internal/poll/poll.go` (Option, Poll+Total, Store, New, Create, Get, Vote) + `poll_test.go` (crear/votar/contar; inválido).
2. **web:** `internal/web/handler.go` (GET /, POST /create, GET /poll/{id}, POST /vote/{id}) + `templates/home.html` + `templates/poll.html` + `static/style.css`.
3. **main.go:** servidor (:8080/PORT).
4. **README + .gitignore.**
5. Verificar build/vet/test; crear+votar de prueba; commits.

Self-review: cobertura del spec ✔ · sin placeholders ✔ · tipos consistentes
(Option, Poll, Store, Create, Get, Vote, Total) ✔.
